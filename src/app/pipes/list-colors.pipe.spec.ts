import { ListColorsPipe } from './list-colors.pipe';

describe('ListColorsPipe', () => {
  it('create an instance', () => {
    const pipe = new ListColorsPipe();
    expect(pipe).toBeTruthy();
  });
});
